1. The value printed by line number 9 is 'values added:  20'. Since add is true we enter the if statement and result is set equal to 10+10=20 so there is no error.
2. The value printed by line number 13 is 'final result:  20'. Result is insantiatied as a var which means it can be accessed throughout the function despite not being in the same block it was created. So result is accessible at line 13 and is still 20. Thus there is no error.
3. Since var has the scope of the function, it exists even before it is created in the function which is confusing. Furthermore, it allows for overrwrites without error so you could accidentally overwrite important values and not know. Lastly since it is function-scoped if you only want a variable to exist only while a a certain condition in the function is true, if it's declared as var that is not possible as long as you are in the function.
4. The values printed by line number 9 is 'values added:  20'. Since add is true we enter the if statement and result is created as a block-scoped variable. It is then set to 10+10=20 so there is no error.
5. This throws an error because let creates a block-scoped variable. Since it is created in the if block it no longer exists after the block ends and so attempting to print result on line 13 throws an error.
6. Line 9 never executes because an error is thrown on line 7, when the code tries to reassign result. Since const variables cannot be reassigned after declaration, the function crashes before reaching line 9.
7. Line 13 never executes because an error is thrown on line 7, when the code tries to reassign result. Since const variables cannot be reassigned after declaration, the function crashes before reaching line 13.
8. 
